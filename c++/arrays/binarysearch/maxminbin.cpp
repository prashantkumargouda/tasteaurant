#include <iostream> 
using namespace std;

int max( int arr[] ) {
    int start = 0 ;
    int end = n - 1;
    int mid = start + ( end - start ) / 2 ;

    while( start <= end ) {
        if ( max )
    }
}